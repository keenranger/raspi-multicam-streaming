# raspi-multicam-streaming
You can observe two webcams. We use motion, to install it type  
`sudo apt-get install motion`  
on your bash  
and type  
`sudo motion -c motion.conf`  
to start.
http://<address>:8085/  
